<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DnDApp.Components.SkillView"
             x:Name="this">
  <ContentView.Content>
        <Grid ColumnSpacing="0" BindingContext="{x:Reference this}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="30" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Frame Grid.Row="0" Grid.Column="0" Padding="15,0,0,0">
                <Label VerticalOptions="Center" HorizontalOptions="StartAndExpand" FontSize="15" Text="{Binding Skill}" />
            </Frame>
            <Frame Grid.Row="0" Grid.Column="1">
                <Label VerticalOptions="Center" HorizontalOptions="Center" FontSize="12" Text="{Binding Score}" />
            </Frame>
            <Frame Grid.Row="0" Grid.Column="2">
                <Label VerticalOptions="Center" HorizontalOptions="Center" FontSize="12" Text="{Binding Modifier}" />
            </Frame>
        </Grid>
  </ContentView.Content>
</ContentView>